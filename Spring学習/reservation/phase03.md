# Phase 03: レイヤ設計（進行中）

## ステータス
🔄 **進行中**

## ブランチ
```bash
git checkout feature/phase03-layer
```

## 作業内容

kakeibo2 と同じ4層構造でパッケージを作成する。

```
com.example.reservation
├── presentation  → Controller, DTO
├── application   → UseCase（@Transactional）
├── domain        → Entity, VO, Repository Interface
└── infrastructure → JPA Entity, Repository 実装
```

---

## 現在の進捗

| 作業 | 状態 |
|------|------|
| ブランチ作成 | ✓ |
| パッケージ構造作成 | 未着手 |
| 各層の役割を理解 | 未着手 |
| PR 作成 | 未着手 |

---

## 理解ポイント

### 質問1: なぜ4層に分けるのか？

kakeibo2 でも学びましたが、復習です。

| 層 | 責務 | 依存していい方向 |
|----|------|-----------------|
| presentation | HTTP リクエスト/レスポンスの変換 | → application |
| application | ビジネスロジックの調整（トランザクション） | → domain |
| domain | ビジネスルール（コアロジック） | なし（最も独立） |
| infrastructure | 外部システム（DB、API）との接続 | → domain |

**ポイント**: domain 層は他の層に依存しない！

### 質問2: Repository インターフェースはどこに置く？

- インターフェース: `domain` 層
- 実装クラス: `infrastructure` 層

**なぜ？** → 依存性逆転の原則（DIP）

---

## 作成するパッケージ

```
src/main/java/com/example/reservation/
├── presentation/
│   ├── controller/
│   └── dto/
├── application/
│   └── usecase/
├── domain/
│   ├── model/
│   │   ├── resource/
│   │   └── reservation/
│   ├── repository/
│   └── exception/
└── infrastructure/
    ├── entity/
    └── repository/
```

---

## 次のタスク

各パッケージに `.gitkeep` ファイルを作成して、フォルダ構造をコミットする。
