```java
	// 未完了のTodoだけ
	List<Todo> openTodos = todos.stream()
	    .filter(todo -> todo.getStatus() == Status.OPEN)
	    .toList();
```

```java
	// ID検索（Listの場合）
	Todo found = todos.stream()
	    .filter(t -> t.getId().equals(id))
	    .findFirst()
	    .orElseThrow();
```

- 件数多い → DB検索
- 件数少ない・一時処理 → Java処理

[filterメソッド,javadoc](https://docs.oracle.com/javase/jp/21/docs/api/java.base/java/util/stream/Stream.html#filter(java.util.function.Predicate))
[findFirstメソッド,javadoc](https://docs.oracle.com/javase/jp/21/docs/api/java.base/java/util/stream/Stream.html#findFirst())
[orElseThrowメソッド,javadoc](https://docs.oracle.com/javase/jp/21/docs/api/java.base/java/util/Optional.html#orElseThrow())


filter：Stream<Todo>
findFist：Optional<Todo>
orElseThrow：Todo



